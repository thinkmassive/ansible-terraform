---
terraform_name: terraform_{{ terraform_version }}
terraform_archive: '{{ terraform_name }}_{{ terraform_platform }}.zip'
terraform_url: '{{terraform_mirror}}/{{terraform_version}}/{{ terraform_archive }}'
terraform_checksum: '{{terraform_checksums[terraform_version][terraform_platform]}}'
